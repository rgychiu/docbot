package user;

public class Utilities {
    /**
     * Method to find a user within the file by ID.
     * @param id id of user
     * @return object of found user from file, null otherwise
     */
    public static User findUser(int id) {
        return null;
    }
}
